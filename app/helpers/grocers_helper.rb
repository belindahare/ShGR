module GrocersHelper
  # def need_list
  #   x= ""
  #   @grocer.needs.each do |need|
  #     x = x + "#{need.item}" +  button_to("I've Got This" , grocer_needs_path(current_user.grocer_id)) + content_tag(:br)
  #   end
  #   x.html_safe
  # end
  
end
